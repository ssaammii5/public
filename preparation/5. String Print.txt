; String Print

.model small
.stack 100h
.data
    m db 'Country BD $'

.code
main proc
    ; Output a character string
    mov ax, @data
    mov ds, ax
    
    mov ah, 9
    lea dx, m
    int 21h
    
    
    ; Exit the program
    mov ah, 4ch
    int 21h

main endp
end main